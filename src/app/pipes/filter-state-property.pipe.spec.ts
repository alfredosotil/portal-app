/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterStatePropertyPipe } from './filter-state-property.pipe';

describe('FilterStatePropertyPipe', () => {
  it('create an instance', () => {
    let pipe = new FilterStatePropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
