/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterTypePropertyPipe } from './filter-type-property.pipe';

describe('FilterTypePropertyPipe', () => {
  it('create an instance', () => {
    let pipe = new FilterTypePropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
